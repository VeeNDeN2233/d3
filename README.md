# Система анализа движений младенцев (GMA)

Веб-приложение для автоматической оценки общих движений младенцев на основе анализа RGB-видео с использованием глубокого обучения.

## 📋 Системные требования

### Минимальные требования

- **Операционная система:** Windows 10/11, Linux (Ubuntu 20.04+), macOS
- **Процессор:** x64 архитектура
- **ОЗУ:** минимум 8 GB (рекомендуется 16 GB)
- **GPU:** NVIDIA GPU с поддержкой CUDA 11.8+ (обязательно для работы приложения)
  - Минимум 4 GB VRAM (рекомендуется 8 GB+)
  - Поддержка CUDA Compute Capability 7.0+
- **Свободное место на диске:** минимум 15 GB (для Docker образа, моделей и зависимостей)

### Необходимое программное обеспечение

#### 1. Docker и Docker Compose

**Проверка установки:**
```bash
docker --version
docker compose version
```

**Установка Docker:**

**Windows:**
1. Скачайте [Docker Desktop для Windows](https://www.docker.com/products/docker-desktop/)
2. Установите Docker Desktop (WSL 2 установится автоматически)
3. **Запустите Docker Desktop** (из меню Пуск или ярлыка)
4. **Дождитесь полного запуска** (иконка в трее перестанет мигать)
5. **Закройте и перезапустите PowerShell/терминал** (это необходимо для обновления переменной PATH)
6. **Проверьте установку:**
   ```bash
   docker --version
   docker compose version
   ```

⚠️ **Важно:** Если команда `docker` не распознается после установки, см. раздел "Решение проблем" ниже.

**Linux (Ubuntu/Debian):**
```bash
# Установка Docker Engine
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Добавьте пользователя в группу docker
sudo usermod -aG docker $USER

# Выйдите и войдите снова, чтобы изменения вступили в силу

# Проверьте установку
docker --version
docker compose version
```

**macOS:**
1. Скачайте [Docker Desktop для Mac](https://www.docker.com/products/docker-desktop/)
2. Установите и запустите Docker Desktop
3. Проверьте установку:
   ```bash
   docker --version
   docker compose version
   ```

**Примечание:** В новых версиях Docker используется команда `docker compose` (без дефиса) вместо `docker-compose`. Если команда `docker compose` не работает, попробуйте `docker-compose` (с дефисом).

#### 2. NVIDIA GPU драйверы

**Проверка GPU:**
```bash
nvidia-smi
# Должна отобразиться информация о GPU и версии CUDA
```

**Установка:**
- Скачайте драйверы с [nvidia.com/drivers](https://www.nvidia.com/Download/index.aspx)
- ⚠️ Версия CUDA драйверов должна поддерживать CUDA 11.8+

#### 3. NVIDIA Container Toolkit (только для Linux)

**Ubuntu/Debian:**
```bash
distribution=$(. /etc/os-release;echo $ID$VERSION_ID)
curl -s -L https://nvidia.github.io/nvidia-docker/gpgkey | sudo apt-key add -
curl -s -L https://nvidia.github.io/nvidia-docker/$distribution/nvidia-docker.list | sudo tee /etc/apt/sources.list.d/nvidia-docker.list

sudo apt-get update && sudo apt-get install -y nvidia-container-toolkit
sudo systemctl restart docker
```

**Windows:**
- Docker Desktop автоматически использует GPU через WSL 2
- Убедитесь, что установлены драйверы NVIDIA для Windows

**macOS:**
- macOS не поддерживает NVIDIA GPU в Docker (только Linux/Windows)

#### 4. Git (для клонирования репозитория)

**Проверка установки:**
```bash
git --version
```

**Установка (если не установлен):**
- **Windows:** [git-scm.com/download/win](https://git-scm.com/download/win)
- **Linux:** `sudo apt install git`
- **macOS:** `brew install git` или установите Xcode Command Line Tools

## 🚀 Развертывание через Docker

### Шаг 1: Клонирование репозитория

```bash
git clone <repository-url>
cd d3
```

### Шаг 2: Подготовка моделей

⚠️ **ВАЖНО:** Модели уже включены в репозиторий, но убедитесь, что файлы находятся в `checkpoints/`:

```bash
# Проверьте наличие файлов
ls checkpoints/best_model_advanced.pt
ls checkpoints/anomaly_detector_advanced.pt
ls checkpoints/normal_statistics.json
```

**Требуемые файлы:**
- `checkpoints/best_model_advanced.pt` - основная модель (~7.6 MB)
- `checkpoints/anomaly_detector_advanced.pt` - детектор аномалий (~2.5 MB)
- `checkpoints/normal_statistics.json` - нормальные статистики для анализа

### Шаг 3: Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```bash
# Скопируйте пример файла (если есть)
cp .env.example .env
```

Или создайте файл `.env` вручную со следующим содержимым:

```env
# Flask Configuration
SECRET_KEY=ваш-сгенерированный-секретный-ключ-64-символа

# Application port
PORT=5000

# Flask mode
FLASK_ENV=production

# Debug mode
FLASK_DEBUG=False
```

**Генерация SECRET_KEY:**

```bash
# Windows PowerShell
python -c "import secrets; print('SECRET_KEY=' + secrets.token_hex(32))"

# Linux/macOS
python3 -c "import secrets; print('SECRET_KEY=' + secrets.token_hex(32))"
```

Скопируйте сгенерированный ключ в файл `.env`.

⚠️ **ВАЖНО:** 
- Используйте уникальный случайный ключ для каждого окружения!
- Файл `.env` уже в `.gitignore` и не будет закоммичен в репозиторий
- Никогда не публикуйте `SECRET_KEY` в открытом доступе

### Шаг 4: Подготовка датасета (опционально, только для обучения)

Если вы планируете обучать модель, вам понадобится датасет MINI-RGBD.

**Структура датасета:**
```
MINI-RGBD_web/
├── 01/
│   └── rgb/
│       ├── syn_0001.png
│       ├── syn_0002.png
│       └── ...
├── 02/
│   └── rgb/
│       └── ...
└── ... (последовательности 01-12)
```

Поместите датасет в родительскую директорию проекта (рядом с папкой `d3`):

```
trae_projects/
├── d3/              # Проект
└── MINI-RGBD_web/   # Датасет
```

Это необходимо, так как `docker-compose.yml` монтирует датасет как volume.

### Шаг 5: Запуск приложения

**Первый запуск (сборка образа):**

```bash
# В корне проекта d3
docker compose up --build
```

Если команда `docker compose` не работает, используйте:

```bash
docker-compose up --build
```

**Запуск в фоновом режиме:**

```bash
docker compose up -d --build
```

**Первый запуск может занять 10-30 минут** из-за:
- Скачивания базового образа CUDA (~3-5 GB)
- Установки всех зависимостей
- Компиляции PyTorch (если требуется)

### Шаг 6: Проверка работы

После успешного запуска:

1. **Откройте браузер:**
   ```
   http://localhost:5000
   ```

2. **Проверьте логи:**
   ```bash
   docker compose logs -f
   ```
   
   Вы должны увидеть сообщения типа:
   ```
   web-1  | * Running on http://0.0.0.0:5000
   web-1  | Модель загружена из checkpoints/best_model_advanced.pt
   web-1  | Детектор загружен из checkpoints/anomaly_detector_advanced.pt
   ```

### Шаг 7: Создание администратора

После первого запуска создайте администратора:

```bash
docker compose exec web python create_admin.py
```

Если команда `docker compose` не работает:

```bash
docker-compose exec web python create_admin.py
```

**Данные для входа по умолчанию:**
- Email: `admin@admin.com`
- Username: `admin`
- Password: `admin123`

⚠️ **ВАЖНО:** Измените пароль администратора после первого входа через админ-панель!

## 📋 Использование

1. **Регистрация/Вход:**
   - Зарегистрируйте новый аккаунт или войдите существующим
   - Администратор: `admin@admin.com` / `admin123`
   - После регистрации вы автоматически войдете в систему

2. **Анализ видео:**
   - Загрузите видео младенца (MP4, AVI, MOV, MKV, WebM)
   - Укажите возраст ребенка и гестационный возраст
   - Запустите анализ
   - Просмотрите результаты и скачайте отчет

3. **Админ-панель:**
   - Доступна для пользователя `admin@admin.com`
   - **Управление пользователями:**
     - Создание новых пользователей
     - Редактирование существующих пользователей (имя, email, роль, статус, пароль)
     - Удаление пользователей
     - Поиск пользователей по имени, email или полному имени
     - Фильтрация по роли (пользователь/администратор) и статусу (активен/заблокирован)
     - Массовые операции: активация, деактивация или удаление нескольких пользователей
     - Статистика: общее количество пользователей, активных, администраторов
   - Просмотр логов системы

## 🐳 Docker команды

**Примечание:** В новых версиях Docker используется `docker compose` (без дефиса). Если команда не работает, попробуйте `docker-compose` (с дефисом).

### Запуск в фоновом режиме:
```bash
docker compose up -d
```

### Просмотр логов:
```bash
# Все логи
docker compose logs -f

# Логи только веб-сервиса
docker compose logs -f web

# Последние 100 строк
docker compose logs --tail=100 web
```

### Остановка:
```bash
docker compose down
```

### Остановка с удалением volumes (⚠️ удалит данные):
```bash
docker compose down -v
```

### Перезапуск:
```bash
docker compose restart
```

### Пересборка образа:
```bash
# С кэшем (быстрее)
docker compose build

# Без кэша (полная пересборка)
docker compose build --no-cache
```

### Выполнение команд в контейнере:
```bash
# Создание администратора
docker compose exec web python create_admin.py

# Вход в контейнер (интерактивный режим)
docker compose exec web bash

# Запуск Python скрипта
docker compose exec web python ваш_скрипт.py
```

### Проверка статуса контейнеров:
```bash
docker compose ps
```

### Просмотр использования ресурсов:
```bash
docker stats
```

### Очистка неиспользуемых данных Docker:
```bash
# Просмотр использования диска
docker system df

# Очистка неиспользуемых образов, контейнеров, volumes
docker system prune -a --volumes
```

## 🔧 Переменные окружения

Файл `.env` содержит конфигурацию приложения. См. "Шаг 3" выше для создания файла.

### Доступные переменные:

- **`SECRET_KEY`** (обязательно) - Секретный ключ для подписи сессий Flask
  - ⚠️ **КРИТИЧЕСКИ ВАЖНО:** Используйте уникальный случайный ключ в production!
  - Генерация ключа:
    ```bash
    python -c "import secrets; print('SECRET_KEY=' + secrets.token_hex(32))"
    ```

- **`PORT`** (опционально) - Порт приложения (по умолчанию: 5000)
  ```env
  PORT=5000
  ```

- **`FLASK_ENV`** (опционально) - Режим Flask
  ```env
  FLASK_ENV=production  # или development
  ```

### Пример файла .env:

```env
# Flask Configuration
SECRET_KEY=ваш-сгенерированный-секретный-ключ-64-символа

# Application port
PORT=5000

# Flask mode
FLASK_ENV=production
```

⚠️ **ВАЖНО:** 
- Файл `.env` не должен быть закоммичен в Git (уже в `.gitignore`)
- Используйте разные `SECRET_KEY` для разных окружений
- Никогда не публикуйте `SECRET_KEY` в открытом доступе

## 📁 Структура проекта

```
d3/
├── app.py                 # Основное Flask приложение
├── flask_analysis.py      # Логика анализа для Flask
├── run_flask.py          # Скрипт запуска
├── create_admin.py       # Скрипт создания администратора
├── train_advanced.py     # Скрипт обучения модели
├── config.yaml           # Конфигурация
├── requirements.txt      # Зависимости Python
├── Dockerfile            # Docker образ
├── docker-compose.yml    # Docker Compose конфигурация
├── .env                  # Переменные окружения (не в Git)
├── auth/                 # Система аутентификации
│   ├── auth_manager.py   # Менеджер аутентификации
│   └── database.py       # База данных пользователей
├── core/                 # Ядро приложения
│   ├── auth_handler.py   # Обработчик аутентификации
│   ├── state_manager.py  # Менеджер состояния
│   └── ...
├── models/               # ML модели
├── utils/                # Утилиты
├── templates/            # HTML шаблоны
│   ├── admin/            # Шаблоны админ-панели
│   │   ├── dashboard.html
│   │   ├── users.html    # Список пользователей
│   │   ├── create_user.html
│   │   └── edit_user.html
│   ├── main.html         # Страница анализа
│   ├── history.html      # История анализов
│   └── ...
├── static/               # CSS и JavaScript
├── checkpoints/          # Сохраненные модели
│   ├── best_model_advanced.pt
│   ├── anomaly_detector_advanced.pt
│   └── normal_statistics.json
├── results/              # Результаты анализа (не в Git)
├── uploads/              # Загруженные видео (не в Git)
└── logs/                 # Логи приложения (не в Git)
```

## 🎓 Обучение модели

Если у вас есть датасет MINI-RGBD и вы хотите обучить модель самостоятельно:

### Требования для обучения:

- Датасет MINI-RGBD в формате проекта
- NVIDIA GPU с минимум 8GB VRAM
- Датасет должен быть смонтирован как volume (см. "Шаг 4" выше)

### Подготовка данных:

1. **Структура датасета:**
```
MINI-RGBD_web/
├── 01/
│   └── rgb/
│       ├── syn_0001.png
│       ├── syn_0002.png
│       └── ...
├── 02/
│   └── rgb/
│       └── ...
└── ... (последовательности 01-12)
```

2. **Настройте config.yaml:**
   - Путь к датасету уже настроен для Docker: `data.mini_rgbd_path = "/app/data/MINI-RGBD_web"`
   - Настройте разделение данных: 
     - `train_sequences: [1, 2, 3, 4, 5, 6, 7, 8]`
     - `val_sequences: [9, 10]`
     - `test_sequences: [11, 12]`
   - Настройте параметры обучения:
     - `training.batch_size: 16` (уменьшите если не хватает памяти)
     - `training.num_epochs: 50` (или больше для лучшего качества)
     - `training.learning_rate: 0.001`

### Запуск обучения через Docker:

1. **Убедитесь, что датасет на месте:**
```bash
# Проверьте структуру датасета (из контейнера)
docker compose exec web ls /app/data/MINI-RGBD_web/01/rgb/syn_0001.png
```

2. **Запустите обучение:**
```bash
# Базовый запуск (использует config.yaml)
docker compose exec web python train_advanced.py

# С указанием конфигурации
docker compose exec web python train_advanced.py --config config.yaml

# Возобновление обучения из checkpoint
docker compose exec web python train_advanced.py --resume checkpoints/checkpoint_epoch_10.pt
```

### Процесс обучения:

Скрипт автоматически:
1. Загрузит данные из датасета MINI-RGBD
2. Извлечет ключевые точки через MediaPipe
3. Обработает их в последовательности
4. Обучит модель Bidirectional LSTM + Attention
5. Вычислит порог аномалий на validation данных
6. Сохранит модель и детектор в `checkpoints/`

### Результаты обучения:

После завершения обучения в папке `checkpoints/` (монтируется как volume) будут созданы:

- **`best_model_advanced.pt`** - основная модель (используется приложением)
- **`anomaly_detector_advanced.pt`** - детектор аномалий с порогом

⚠️ **ВАЖНО:** Для корректной работы приложения файлы должны называться:
- `checkpoints/best_model_advanced.pt` - модель
- `checkpoints/anomaly_detector_advanced.pt` - детектор

### Мониторинг обучения:

Во время обучения вы увидите:
```
Epoch 1/50 - Train Loss: 0.123456, Val Loss: 0.098765, LR: 0.001000
Новая лучшая модель! Val Loss: 0.098765
...
```

Лучшая модель сохраняется автоматически при улучшении validation loss.

Для просмотра логов обучения:
```bash
docker compose logs -f web
```

### Время обучения:

- Зависит от размера датасета и GPU
- Обычно 2-6 часов на NVIDIA RTX 3080/3090
- Можно уменьшить `max_frames_per_seq` в `config.yaml` для быстрого теста

## 🐛 Решение проблем

### Проблема: "docker не распознано как имя командлета" (Windows)

Если после установки Docker Desktop команда `docker` не распознается в PowerShell:

**Решение:**
1. **Запустите Docker Desktop** (из меню Пуск или ярлыка)
2. **Дождитесь полного запуска** (иконка в трее перестанет мигать)
3. **Закройте и перезапустите PowerShell/терминал** (это необходимо для обновления переменной PATH)
4. **Проверьте, что Docker доступен:**
   ```bash
   docker --version
   docker compose version
   ```

Если проблема сохраняется после перезапуска:
- Убедитесь, что Docker Desktop полностью запущен (иконка в системном трее)
- Проверьте, что Docker Desktop добавлен в PATH автоматически
- Попробуйте перезагрузить компьютер после установки Docker Desktop

### Проблема: Docker не запускается (Windows)

**Решение:**
1. Убедитесь, что установлен WSL 2
2. Включите виртуализацию в BIOS
3. Перезапустите Docker Desktop
4. Проверьте, что Hyper-V включен (для Windows Pro/Enterprise)

### Проблема: "Cannot connect to the Docker daemon"

**Решение:**
1. Убедитесь, что Docker Desktop запущен
2. На Linux проверьте, что служба Docker запущена:
   ```bash
   sudo systemctl status docker
   sudo systemctl start docker
   ```
3. Проверьте права доступа (на Linux добавьте пользователя в группу docker):
   ```bash
   sudo usermod -aG docker $USER
   # Выйдите и войдите снова
   ```

### Проблема: "CUDA is not available" или "No GPU found"

**Решение:**
1. Убедитесь, что драйверы NVIDIA установлены: `nvidia-smi`
2. Проверьте, что GPU доступен в Docker:
   ```bash
   # Linux
   docker run --rm --gpus all nvidia/cuda:11.8.0-base-ubuntu22.04 nvidia-smi
   
   # Windows (Docker Desktop)
   # GPU должен быть доступен автоматически через WSL 2
   ```
3. На Linux установите NVIDIA Container Toolkit (см. раздел "NVIDIA Container Toolkit" выше)
4. Проверьте логи контейнера:
   ```bash
   docker compose logs web | grep -i cuda
   ```

### Проблема: "Out of memory" при обучении

**Решение:**
1. Уменьшите `batch_size` в `config.yaml`
2. Закройте другие приложения, использующие GPU
3. Уменьшите `max_frames_per_seq` в `config.yaml` для тестирования
4. Используйте GPU с большим объемом VRAM

### Проблема: Модели не загружаются

**Решение:**
1. Проверьте, что файлы находятся в `checkpoints/`:
   ```bash
   docker compose exec web ls -lh checkpoints/
   ```
2. Убедитесь, что файлы имеют правильные имена:
   - `checkpoints/best_model_advanced.pt`
   - `checkpoints/anomaly_detector_advanced.pt`
3. Проверьте логи:
   ```bash
   docker compose logs web | grep -i "модель\|model"
   ```

### Проблема: Датасет не найден при обучении

**Решение:**
1. Убедитесь, что датасет находится в родительской директории проекта
2. Проверьте, что путь в `config.yaml` правильный: `data.mini_rgbd_path = "/app/data/MINI-RGBD_web"`
3. Проверьте, что датасет смонтирован:
   ```bash
   docker compose exec web ls /app/data/MINI-RGBD_web/
   ```
4. Убедитесь, что в `docker-compose.yml` есть volume для датасета:
   ```yaml
   volumes:
     - ../MINI-RGBD_web:/app/data/MINI-RGBD_web
   ```

### Проблема: Порт 5000 уже занят

**Решение:**
1. Измените порт в `.env`:
   ```env
   PORT=5001
   ```
2. Или остановите приложение, использующее порт 5000
3. Перезапустите контейнер:
   ```bash
   docker compose down
   docker compose up -d
   ```

### Проблема: Недостаточно места на диске

**Решение:**
1. Очистите неиспользуемые Docker данные:
   ```bash
   docker system prune -a --volumes
   ```
2. Удалите неиспользуемые образы:
   ```bash
   docker image prune -a
   ```
3. Освободите место на диске (удалите старые результаты, логи и т.д.)

## ⚠️ Важные замечания

1. **Модели включены в репозиторий** - файлы находятся в `checkpoints/` и уже готовы к использованию
2. **База данных пользователей** - `auth/users.db` не включена в репозиторий, создастся автоматически при первом запуске
3. **SECRET_KEY** - обязательно измените `SECRET_KEY` в `.env` или переменных окружения для production
4. **GPU обязателен** - приложение требует NVIDIA GPU с CUDA поддержкой
5. **Пароль администратора** - измените пароль администратора (`admin123`) после первого входа через админ-панель
6. **База данных** - использует SQLite с WAL режимом для лучшей производительности
7. **Аутентификация** - пароли хешируются с помощью bcrypt (12 раундов)
8. **Volumes** - данные в volumes (checkpoints, results, uploads, logs) сохраняются между перезапусками контейнера

## 📝 Примечания

- Первый запуск может занять время из-за загрузки Docker образа и установки зависимостей
- Убедитесь, что GPU доступен: `nvidia-smi`
- Модели должны быть в директории `checkpoints/` (уже включены в репозиторий)
- Результаты сохраняются в `results/` (монтируется как volume)
- Логи доступны через `docker compose logs -f web`

## 📄 Лицензия

См. файл LICENSE в корне проекта.
