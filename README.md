# 🍼 Детектор аномалий движений младенцев

Система для раннего выявления нарушений моторики у младенцев на основе анализа RGB-видео с использованием глубокого обучения.

## 🎯 Основные возможности

- ✅ **Анализ RGB-видео** - обработка видео младенцев без необходимости 3D данных
- ✅ **Детекция аномалий** - автоматическое выявление паттернов движений, отклоняющихся от нормы
- ✅ **Веб-интерфейс** - удобный Gradio интерфейс для загрузки и анализа видео
- ✅ **Медицинские отчеты** - детальная статистика и рекомендации
- ✅ **GPU ускорение** - полная поддержка CUDA для быстрой обработки

## 🏗️ Архитектура

Система использует **Bidirectional LSTM + Attention** автоэнкодер для детекции аномалий:

```
RGB видео → MediaPipe (33 точки) → Преобразование формата → 
Нормализация → LSTM Autoencoder → Оценка аномалии
```

### Ключевые компоненты:

1. **MediaPipe Pose** - извлечение ключевых точек (33 точки)
2. **Pose Processor** - преобразование в формат MINI-RGBD (25 суставов) с нормализацией
3. **Bidirectional LSTM Autoencoder** - модель для реконструкции нормальных движений
4. **Anomaly Detector** - детектор аномалий на основе ошибки реконструкции

## 📋 Требования

- Python 3.8+
- CUDA-capable GPU (рекомендуется)
- ~2GB свободного места для моделей

## 🚀 Установка

### 1. Клонируйте репозиторий

```bash
git clone <repository-url>
cd d3
```

### 2. Создайте виртуальное окружение

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Подготовьте данные

Поместите датасет MINI-RGBD в папку `MINI-RGBD_web/` (структура должна соответствовать формату датасета).

## 📖 Использование

### Веб-интерфейс (рекомендуется)

```bash
python medical_interface.py
```

Откройте браузер и перейдите по адресу: `http://127.0.0.1:7861`

**Инструкция:**
1. Нажмите "Загрузить модели" для инициализации системы
2. Загрузите видео файл (MP4, AVI, MOV, MKV)
3. Нажмите "Анализировать видео"
4. Просмотрите результаты и медицинский отчет

### Командная строка

```bash
python inference_advanced.py \
  --video test_videos/baby.mp4 \
  --checkpoint checkpoints/best_model_advanced.pt \
  --output results/analysis \
  --save_report
```

## 🎓 Обучение модели

### Обучение улучшенной модели (Bidirectional LSTM + Attention)

```bash
python train_advanced.py --config config.yaml --model bidir_lstm
```

### Обучение базовой модели (для справки)

```bash
python train_gpu.py --config config.yaml
```

## 📊 Результаты

### Производительность улучшенной модели:

| Метрика | Значение |
|---------|----------|
| **Val Loss** | 0.013457 |
| **Порог аномалии** | 0.020204 |
| **Параметров** | 2,212,032 |
| **Mean Error (baby.mp4)** | 0.006339 |
| **Anomaly Rate** | 0% |

### Сравнение с базовой моделью:

| Модель | Mean Error | Anomaly Rate | Risk Level |
|--------|-----------|--------------|------------|
| Базовая LSTM | 2.997 | 100% | MEDIUM |
| **Bidirectional LSTM + Attention** | **0.006** | **0%** | **LOW** |

## 📁 Структура проекта

```
d3/
├── models/                    # Модели нейросетей
│   ├── autoencoder_advanced.py # Улучшенная модель (Bidirectional LSTM + Attention)
│   ├── autoencoder_gpu.py     # Базовая модель
│   └── anomaly_detector.py    # Детектор аномалий
├── utils/                      # Утилиты
│   ├── data_loader.py         # Загрузка данных MINI-RGBD
│   ├── pose_processor.py      # Обработка поз
│   └── data_augmentation.py   # Аугментация данных
├── checkpoints/               # Сохраненные модели
│   ├── best_model_advanced.pt # Основная модель
│   └── anomaly_detector_advanced.pt
├── medical_interface.py       # Веб-интерфейс (Gradio)
├── inference_advanced.py      # Инференс улучшенной модели
├── train_advanced.py          # Обучение улучшенной модели
├── train_gpu.py               # Обучение базовой модели
├── video_processor.py         # Обработка видео (MediaPipe)
├── config.yaml                # Конфигурация
└── requirements.txt           # Зависимости
```

## ⚙️ Конфигурация

Основные параметры настраиваются в `config.yaml`:

- **Модель**: архитектура, размеры скрытых слоев, dropout
- **Обучение**: batch size, learning rate, количество эпох
- **Данные**: пути к датасету, разделение train/val/test
- **Pose**: параметры MediaPipe, нормализация
- **Аугментация**: шум, асимметрия, вариации

## 🔬 Технические детали

### Нормализация

Система использует улучшенную нормализацию:
- **Нормализация по bounding box** - масштабирование относительно размеров тела
- **Вращение к канонической ориентации** - компенсация угла съемки
- **Относительно торса** - центрирование относительно центра торса

### Аугментация данных

Для улучшения обобщения применяется:
- Добавление шума (как в реальных видео)
- Асимметрия движений
- Естественная изменчивость
- Небольшие вращения и масштабирования

## 📝 Лицензия

Проект использует:
- **MediaPipe** - Apache 2.0
- **PyTorch** - BSD-style
- **Gradio** - Apache 2.0

## ⚠️ Важно

- Система предназначена для **вспомогательной диагностики**
- Результаты **не заменяют** консультацию специалиста
- При обнаружении аномалий рекомендуется обратиться к врачу

## 📚 Дополнительная документация

Архивная документация находится в папке `docs/archive/`:
- Результаты экспериментов
- Диагностические отчеты
- Сравнения моделей
- История разработки

## 🤝 Вклад

Проект разработан для исследования раннего выявления нарушений моторики у младенцев.

## 📧 Контакты

Для вопросов и предложений создайте issue в репозитории.
