# Система анализа движений младенцев (GMA)

Веб-приложение для автоматической оценки общих движений младенцев на основе анализа RGB-видео с использованием глубокого обучения.

## 📋 Системные требования и установка

### Минимальные требования

- **Операционная система:** Windows 10/11, Linux (Ubuntu 20.04+), macOS
- **Процессор:** x64 архитектура
- **ОЗУ:** минимум 8 GB (рекомендуется 16 GB)
- **GPU:** NVIDIA GPU с поддержкой CUDA 11.8+ (обязательно для работы приложения)
  - Минимум 4 GB VRAM (рекомендуется 8 GB+)
  - Поддержка CUDA Compute Capability 7.0+
- **Свободное место на диске:** минимум 10 GB (для моделей, зависимостей и данных)

### Необходимое программное обеспечение

#### 1. Python 3.11 или выше

**Проверка установки:**
```bash
python --version
# Должно быть: Python 3.11.x или выше
```

**Установка (если не установлен):**
- **Windows:** Скачайте с [python.org](https://www.python.org/downloads/)
  - ⚠️ При установке отметьте "Add Python to PATH"
- **Linux (Ubuntu/Debian):**
  ```bash
  sudo apt update
  sudo apt install python3.11 python3.11-venv python3-pip
  ```
- **macOS:**
  ```bash
  brew install python@3.11
  ```

#### 2. NVIDIA GPU драйверы и CUDA Toolkit

**Проверка GPU:**
```bash
nvidia-smi
# Должна отобразиться информация о GPU и версии CUDA
```

**Установка:**
- Скачайте драйверы с [nvidia.com/drivers](https://www.nvidia.com/Download/index.aspx)
- Установите CUDA Toolkit 11.8+ с [developer.nvidia.com/cuda-downloads](https://developer.nvidia.com/cuda-downloads)
- ⚠️ Версия CUDA должна соответствовать версии PyTorch (см. ниже)

#### 3. Git (для клонирования репозитория)

**Проверка установки:**
```bash
git --version
```

**Установка (если не установлен):**
- **Windows:** [git-scm.com/download/win](https://git-scm.com/download/win)
- **Linux:** `sudo apt install git`
- **macOS:** `brew install git` или установите Xcode Command Line Tools

#### 4. Docker и Docker Compose (опционально, для контейнеризации)

**Проверка установки:**
```bash
docker --version
docker compose version
```

**Установка:**
- **Windows:** [Docker Desktop](https://www.docker.com/products/docker-desktop/)
  - Требуется WSL 2 (устанавливается автоматически с Docker Desktop)
- **Linux:**
  ```bash
  # Docker Engine
  curl -fsSL https://get.docker.com -o get-docker.sh
  sudo sh get-docker.sh
  
  # Docker Compose (в новых версиях входит в Docker)
  sudo apt install docker-compose-plugin
  ```
- **macOS:** [Docker Desktop](https://www.docker.com/products/docker-desktop/)

**Примечание:** В новых версиях Docker используется команда `docker compose` (без дефиса) вместо `docker-compose`.

### Установка зависимостей Python

#### Вариант 1: Локальная установка (рекомендуется для разработки)

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/VeeNDeN2233/d3.git
   cd d3
   ```

2. **Создайте виртуальное окружение:**
   ```bash
   # Windows
   python -m venv venv
   venv\Scripts\activate
   
   # Linux/macOS
   python3 -m venv venv
   source venv/bin/activate
   ```

3. **Обновите pip:**
   ```bash
   pip install --upgrade pip
   ```

4. **Установите PyTorch с CUDA поддержкой:**
   ```bash
   # Для CUDA 11.8
   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
   
   # Для CUDA 12.1
   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
   
   # Проверьте установку
   python -c "import torch; print(f'PyTorch: {torch.__version__}'); print(f'CUDA доступна: {torch.cuda.is_available()}')"
   ```

5. **Установите остальные зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

6. **Настройте переменные окружения:**
   ```bash
   # Скопируйте пример файла
   cp .env.example .env
   
   # Сгенерируйте SECRET_KEY
   python -c "import secrets; print('SECRET_KEY=' + secrets.token_hex(32))"
   
   # Откройте .env и вставьте сгенерированный SECRET_KEY
   ```

7. **Поместите модели в checkpoints:**
   - Убедитесь, что файлы находятся в `checkpoints/`:
     - `checkpoints/best_model_advanced.pt`
     - `checkpoints/anomaly_detector_advanced.pt`
   - Модели не включены в репозиторий из-за размера

8. **Запустите приложение:**
   ```bash
   python run_flask.py
   ```
   
   Приложение будет доступно по адресу: `http://localhost:5000`

#### Вариант 2: Установка через Docker (для production)

См. раздел "🚀 Быстрый старт с Docker" ниже.

### Проверка установки

После установки всех зависимостей выполните проверку:

```bash
# Проверка Python
python --version

# Проверка GPU и CUDA
nvidia-smi

# Проверка PyTorch и CUDA
python -c "import torch; print('PyTorch:', torch.__version__); print('CUDA доступна:', torch.cuda.is_available()); print('CUDA версия:', torch.version.cuda if torch.cuda.is_available() else 'N/A')"

# Проверка основных зависимостей
python -c "import flask, cv2, mediapipe, numpy, yaml; print('Все зависимости установлены успешно!')"
```

### Решение проблем

#### Проблема: "CUDA is not available" в PyTorch

**Решение:**
1. Убедитесь, что установлены драйверы NVIDIA: `nvidia-smi`
2. Проверьте версию CUDA: `nvcc --version`
3. Установите PyTorch с правильной версией CUDA (см. выше)
4. Перезапустите терминал/IDE

#### Проблема: "No module named 'torch'" или другие импорты

**Решение:**
1. Убедитесь, что виртуальное окружение активировано
2. Переустановите зависимости: `pip install -r requirements.txt`

#### Проблема: Недостаточно памяти GPU

**Решение:**
1. Уменьшите `batch_size` в `config.yaml`
2. Закройте другие приложения, использующие GPU
3. Используйте GPU с большим объемом VRAM

#### Проблема: Docker не запускается (Windows)

**Решение:**
1. Убедитесь, что установлен WSL 2
2. Включите виртуализацию в BIOS
3. Перезапустите Docker Desktop

## 🚀 Быстрый старт с Docker

### Требования

- Docker и Docker Compose
- NVIDIA GPU с поддержкой CUDA 11.8+
- NVIDIA Container Toolkit

### Установка NVIDIA Container Toolkit

**Ubuntu/Debian:**
```bash
distribution=$(. /etc/os-release;echo $ID$VERSION_ID)
curl -s -L https://nvidia.github.io/nvidia-docker/gpgkey | sudo apt-key add -
curl -s -L https://nvidia.github.io/nvidia-docker/$distribution/nvidia-docker.list | sudo tee /etc/apt/sources.list.d/nvidia-docker.list

sudo apt-get update && sudo apt-get install -y nvidia-container-toolkit
sudo systemctl restart docker
```

**Windows:**
Установите WSL2 и следуйте инструкциям для Ubuntu, или используйте Docker Desktop с поддержкой WSL2.

### Запуск приложения

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd d3
```

2. **Поместите модель в checkpoints:**
```bash
# Убедитесь, что файлы находятся в checkpoints/
# Модели не включены в репозиторий из-за размера (обычно 100-500 MB)
# Вы должны получить их отдельно (от автора проекта или обучить самостоятельно)
ls checkpoints/best_model_advanced.pt
ls checkpoints/anomaly_detector_advanced.pt
```

**⚠️ ВАЖНО:** Для корректной работы приложения файлы должны называться:
- `checkpoints/best_model_advanced.pt` - основная модель
- `checkpoints/anomaly_detector_advanced.pt` - детектор аномалий с порогом

**Почему модели не в репозитории?**
- Файлы моделей очень большие (сотни мегабайт)
- Git не предназначен для больших бинарных файлов
- Это стандартная практика для ML проектов
- Модели можно получить отдельно или обучить самостоятельно (см. раздел "🎓 Обучение модели" ниже)

3. **Настройте переменные окружения (опционально):**
```bash
# Скопируйте пример файла и измените SECRET_KEY
cp .env.example .env
# Отредактируйте .env и установите свой SECRET_KEY
```

4. **Запустите Docker Compose:**
```bash
docker-compose up --build
```

5. **Откройте браузер:**
```
http://localhost:5000
```

### Создание администратора

После первого запуска создайте администратора:

```bash
docker-compose exec web python create_admin.py
```

Или запустите локально (если Python установлен):
```bash
python create_admin.py
```

**Данные для входа по умолчанию:**
- Email: `admin@admin.com`
- Username: `admin`
- Password: `admin123`

⚠️ **ВАЖНО:** Измените пароль администратора после первого входа через админ-панель!

## 📋 Использование

1. **Регистрация/Вход:**
   - Зарегистрируйте новый аккаунт или войдите существующим
   - Администратор: `admin@admin.com` / `admin123`
   - После регистрации вы автоматически войдете в систему

2. **Анализ видео:**
   - Загрузите видео младенца (MP4, AVI, MOV, MKV, WebM)
   - Укажите возраст ребенка и гестационный возраст
   - Запустите анализ
   - Просмотрите результаты и скачайте отчет

3. **Админ-панель:**
   - Доступна для пользователя `admin@admin.com`
   - **Управление пользователями:**
     - Создание новых пользователей
     - Редактирование существующих пользователей (имя, email, роль, статус, пароль)
     - Удаление пользователей
     - Поиск пользователей по имени, email или полному имени
     - Фильтрация по роли (пользователь/администратор) и статусу (активен/заблокирован)
     - Массовые операции: активация, деактивация или удаление нескольких пользователей
     - Статистика: общее количество пользователей, активных, администраторов
   - Просмотр логов системы

## 🐳 Docker команды

### Запуск в фоновом режиме:
```bash
docker-compose up -d
```

### Просмотр логов:
```bash
docker-compose logs -f
```

### Остановка:
```bash
docker-compose down
```

### Пересборка образа:
```bash
docker-compose build --no-cache
```

### Выполнение команд в контейнере:
```bash
docker-compose exec web python create_admin.py
```

## 📁 Структура проекта

```
d3/
├── app.py                 # Основное Flask приложение
├── flask_analysis.py      # Логика анализа для Flask
├── run_flask.py          # Скрипт запуска
├── create_admin.py       # Скрипт создания администратора
├── train_advanced.py     # Скрипт обучения модели
├── config.yaml           # Конфигурация
├── requirements.txt      # Зависимости Python
├── Dockerfile            # Docker образ
├── docker-compose.yml    # Docker Compose конфигурация
├── auth/                 # Система аутентификации
│   ├── auth_manager.py   # Менеджер аутентификации
│   └── database.py       # База данных пользователей
├── core/                 # Ядро приложения
│   ├── auth_handler.py   # Обработчик аутентификации
│   ├── state_manager.py  # Менеджер состояния
│   └── ...
├── models/               # ML модели
├── utils/                # Утилиты
├── templates/            # HTML шаблоны
│   ├── admin/            # Шаблоны админ-панели
│   │   ├── dashboard.html
│   │   ├── users.html    # Список пользователей
│   │   ├── create_user.html
│   │   └── edit_user.html
│   ├── main.html         # Страница анализа
│   ├── history.html      # История анализов
│   └── ...
├── static/               # CSS и JavaScript
├── checkpoints/          # Сохраненные модели (не в Git)
├── results/              # Результаты анализа (не в Git)
├── uploads/              # Загруженные видео (не в Git)
└── logs/                 # Логи приложения (не в Git)
```

## 🔧 Переменные окружения

Файл `.env` содержит конфигурацию приложения. Скопируйте `.env.example` в `.env` и настройте значения:

```bash
cp .env.example .env
```

### Доступные переменные:

- **`SECRET_KEY`** (обязательно) - Секретный ключ для подписи сессий Flask
  - ⚠️ **КРИТИЧЕСКИ ВАЖНО:** Используйте уникальный случайный ключ в production!
  - Генерация ключа:
    ```bash
    python -c "import secrets; print('SECRET_KEY=' + secrets.token_hex(32))"
    ```
  - Или используйте `secrets.token_urlsafe(32)` для URL-safe версии

- **`PORT`** (опционально) - Порт приложения (по умолчанию: 5000)
  ```env
  PORT=5000
  ```

- **`FLASK_ENV`** (опционально) - Режим Flask
  ```env
  FLASK_ENV=production  # или development
  ```

- **`FLASK_DEBUG`** (опционально) - Режим отладки
  ```env
  FLASK_DEBUG=False  # True для разработки, False для production
  ```

- **`FLASK_HOST`** (опционально) - Хост для привязки
  ```env
  FLASK_HOST=0.0.0.0  # для всех интерфейсов, или 127.0.0.1 для localhost
  ```

### Пример файла .env:

```env
# Flask Configuration
SECRET_KEY=ваш-сгенерированный-секретный-ключ-64-символа

# Application port
PORT=5000

# Flask mode
FLASK_ENV=production

# Debug mode
FLASK_DEBUG=False

# Host to bind
FLASK_HOST=0.0.0.0
```

⚠️ **ВАЖНО:** 
- Файл `.env` не должен быть закоммичен в Git (уже в `.gitignore`)
- Используйте разные `SECRET_KEY` для разных окружений
- Никогда не публикуйте `SECRET_KEY` в открытом доступе

## 📝 Примечания

- Первый запуск может занять время из-за загрузки моделей
- Убедитесь, что GPU доступен: `nvidia-smi`
- Модели должны быть в директории `checkpoints/`
- Результаты сохраняются в `results/`

## 🎓 Обучение модели

Если у вас есть датасет MINI-RGBD и вы хотите обучить модель самостоятельно:

### Требования для обучения:

- Датасет MINI-RGBD в формате проекта
- NVIDIA GPU с минимум 8GB VRAM
- Python 3.11+ с установленными зависимостями
- Все зависимости из `requirements.txt`

### Подготовка данных:

1. **Структура датасета:**
```
MINI-RGBD_web/
├── 01/
│   └── rgb/
│       ├── syn_0001.png
│       ├── syn_0002.png
│       └── ...
├── 02/
│   └── rgb/
│       └── ...
└── ... (последовательности 01-12)
```

2. **Настройте config.yaml:**
   - Укажите путь к датасету: `data.mini_rgbd_path = "MINI-RGBD_web"`
   - Настройте разделение данных: 
     - `train_sequences: [1, 2, 3, 4, 5, 6, 7, 8]`
     - `val_sequences: [9, 10]`
     - `test_sequences: [11, 12]`
   - Настройте параметры обучения:
     - `training.batch_size: 16` (уменьшите если не хватает памяти)
     - `training.num_epochs: 50` (или больше для лучшего качества)
     - `training.learning_rate: 0.001`

### Запуск обучения:

1. **Убедитесь, что датасет на месте:**
```bash
# Проверьте структуру датасета
ls MINI-RGBD_web/01/rgb/syn_0001.png
```

2. **Запустите скрипт обучения:**
```bash
python train_advanced.py --config config.yaml --checkpoint checkpoints/best_model_advanced.pt
```

3. **Параметры скрипта:**
```bash
# Базовый запуск (использует config.yaml и checkpoints/best_model_advanced.pt)
python train_advanced.py

# С указанием конфигурации
python train_advanced.py --config config.yaml

# С указанием пути для сохранения модели
python train_advanced.py --checkpoint checkpoints/my_model.pt

# Возобновление обучения из checkpoint
python train_advanced.py --resume checkpoints/checkpoint_epoch_10.pt
```

**⚠️ ВАЖНО:** После завершения обучения убедитесь, что файлы имеют правильные имена:
- `checkpoints/best_model_advanced.pt` - основная модель
- `checkpoints/anomaly_detector_advanced.pt` - детектор аномалий

Если вы использовали другое имя в `--checkpoint`, переименуйте файлы или скопируйте их с правильными именами.

### Процесс обучения:

Скрипт автоматически:
1. Загрузит данные из датасета MINI-RGBD
2. Извлечет ключевые точки через MediaPipe
3. Обработает их в последовательности
4. Обучит модель Bidirectional LSTM + Attention
5. Вычислит порог аномалий на validation данных
6. Сохранит модель и детектор

### Результаты обучения:

После завершения обучения в папке `checkpoints/` будут созданы:

- **`best_model_advanced.pt`** - основная модель (используется приложением)
- **`anomaly_detector_advanced.pt`** - детектор аномалий с порогом

**⚠️ ВАЖНО:** Для корректной работы приложения файлы должны называться:
- `checkpoints/best_model_advanced.pt` - модель
- `checkpoints/anomaly_detector_advanced.pt` - детектор

Если вы использовали другое имя, переименуйте файлы или измените путь в `config.yaml` и `flask_analysis.py`.

### Мониторинг обучения:

Во время обучения вы увидите:
```
Epoch 1/50 - Train Loss: 0.123456, Val Loss: 0.098765, LR: 0.001000
Новая лучшая модель! Val Loss: 0.098765
...
```

Лучшая модель сохраняется автоматически при улучшении validation loss.

### Время обучения:

- Зависит от размера датасета и GPU
- Обычно 2-6 часов на NVIDIA RTX 3080/3090
- Можно уменьшить `max_frames_per_seq` в `config.yaml` для быстрого теста

## 🛠️ Разработка

### Локальная установка (без Docker)

Для разработки рекомендуется использовать локальную установку (см. раздел "📋 Системные требования и установка" выше).

**Краткая инструкция:**

1. Убедитесь, что установлены все системные требования (Python 3.11+, NVIDIA GPU, CUDA)
2. Создайте и активируйте виртуальное окружение
3. Установите PyTorch с CUDA поддержкой
4. Установите зависимости из `requirements.txt`
5. Настройте `.env` файл с `SECRET_KEY`
6. Поместите модели в `checkpoints/`
7. Запустите: `python run_flask.py`

**Для создания администратора:**
```bash
python create_admin.py
```

**Для запуска в режиме разработки:**
```bash
# Установите FLASK_DEBUG=True в .env или:
export FLASK_DEBUG=True  # Linux/macOS
set FLASK_DEBUG=True     # Windows PowerShell
$env:FLASK_DEBUG="True"  # Windows PowerShell (альтернатива)

python run_flask.py
```

## ⚠️ Важные замечания перед публикацией

1. **Модели не включены в репозиторий** - файлы `.pt` должны быть добавлены вручную в `checkpoints/`
2. **База данных пользователей** - `auth/users.db` не включена в репозиторий, создастся автоматически при первом запуске
3. **SECRET_KEY** - обязательно измените `SECRET_KEY` в `.env` или переменных окружения для production
4. **GPU обязателен** - приложение требует NVIDIA GPU с CUDA поддержкой
5. **Пароль администратора** - измените пароль администратора (`admin123`) после первого входа через админ-панель
6. **База данных** - использует SQLite с WAL режимом для лучшей производительности
7. **Аутентификация** - пароли хешируются с помощью bcrypt (12 раундов)

## 📄 Лицензия

См. файл LICENSE в корне проекта.
