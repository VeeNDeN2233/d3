# Руководство по развертыванию

## 🐳 Развертывание через Docker

### Быстрый старт

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd d3
```

2. **Поместите модели в checkpoints:**
```bash
# Скопируйте файлы моделей в checkpoints/
cp /path/to/best_model_advanced.pt checkpoints/
cp /path/to/anomaly_detector_advanced.pt checkpoints/
```

3. **Настройте переменные окружения:**
```bash
cp .env.example .env
# Отредактируйте .env и установите SECRET_KEY
```

4. **Запустите:**
```bash
docker-compose up --build
```

5. **Создайте администратора:**
```bash
docker-compose exec web python create_admin.py
```

6. **Откройте браузер:**
```
http://localhost:5000
```

## 📋 Чеклист перед публикацией

- [x] Все ненужные файлы удалены
- [x] `.gitignore` настроен правильно
- [x] База данных исключена из Git
- [x] Модели исключены из Git
- [x] Docker конфигурация создана
- [x] README с инструкциями
- [x] LICENSE файл создан
- [x] `.env.example` создан
- [x] Скрипт обучения восстановлен
- [x] Все пути и имена файлов проверены
- [x] Полный функционал админ-панели реализован
- [x] Система управления пользователями (CRUD) работает
- [x] Поиск и фильтрация пользователей реализованы
- [x] Массовые операции для пользователей работают
- [x] История анализов с возможностью удаления реализована

## ⚠️ Важные моменты

1. **SECRET_KEY** - обязательно измените в production
2. **Модели** - должны быть добавлены вручную
3. **GPU обязателен** - приложение требует NVIDIA GPU
4. **Администратор** - создается через `create_admin.py` (email: admin@admin.com, password: admin123)
5. **Безопасность** - пароли хешируются с помощью bcrypt, SQL-инъекции защищены через параметризованные запросы
6. **Админ-панель** - полный функционал управления пользователями доступен после входа администратора

## 🔒 Безопасность

- База данных не включена в репозиторий
- `.env` файлы исключены из Git
- SECRET_KEY имеет дефолтное значение - измените в production
- Пароль администратора по умолчанию - измените после первого входа
