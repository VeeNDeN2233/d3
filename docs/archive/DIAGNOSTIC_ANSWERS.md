# ОТВЕТЫ НА ВОПРОСЫ ДЛЯ ДИАГНОСТИКИ СИСТЕМЫ

## 1. ПРО ТРЕНИРОВКУ МОДЕЛИ

### На каких данных обучался автоэнкодер?
- **Источник**: RGB изображения из MINI-RGBD датасета
- **Последовательности**: 01-08 (train), 09-10 (val), 11-12 (test)
- **Ограничение**: 100 кадров на последовательность (тестовый режим)
- **Всего для обучения**: 800 кадров → 737 последовательностей
- **Формат данных**: RGB изображения (PNG) из папок `MINI-RGBD_web/XX/rgb/syn_*.png`

### Размер входа (input_size)
- **input_size = 75** (25 суставов × 3 координаты: x, y, z)
- **sequence_length = 30** кадров
- **Форма входных данных**: (N, 30, 75) где N - количество последовательностей

### Какие суставы использовались?
- **25 суставов** в формате MINI-RGBD:
  - global, leftThigh, rightThigh, spine, leftCalf, rightCalf, spine1
  - leftFoot, rightFoot, spine2, leftToes, rightToes, neck
  - leftShoulder, rightShoulder, head, leftUpperArm, rightUpperArm
  - leftForeArm, rightForeArm, leftHand, rightHand, leftFingers, rightFingers, noseVertex

### Нормализация данных
- **Включена**: `normalize: true`
- **Тип**: Относительно торса (`normalize_relative_to: "torso"`)
- **Алгоритм**:
  1. Центрирование: `pose = pose - torso_center` (где torso_center = средняя точка между бедрами)
  2. Масштабирование: `pose = pose / hip_distance` (расстояние между бедрами как масштаб)
- **Код**: `utils/pose_processor.py:normalize_pose()` (строки 180-206)

### Loss во время тренировки
- **Train loss**: 
  - Epoch 1: 2.362038
  - Epoch 4 (best): 0.987262
  - Epoch 10: 0.976109
- **Val loss**:
  - Epoch 1: 1.692529
  - Epoch 4 (best): 1.690814
  - Epoch 10: 1.698445
- **Best val loss**: 1.690814 (Epoch 4)
- **Loss функция**: MSE (Mean Squared Error)

---

## 2. ПРО ДАННЫЕ

### Как загружались данные из MINI-RGBD?
- **Загрузка**: RGB изображения через `utils/data_loader.py`
- **Процесс**:
  1. Загрузка PNG изображений из `MINI-RGBD_web/XX/rgb/`
  2. Извлечение ключевых точек через **MediaPipe Pose** (33 точки)
  3. Преобразование MediaPipe → MINI-RGBD (25 суставов)
  4. Нормализация относительно торса
  5. Создание последовательностей длиной 30 кадров
- **НЕ использовались**: готовые ключевые точки из MINI-RGBD (joints_2Ddep, joints_3D)

### Преобразование MediaPipe → MINI-RGBD
- **Да, преобразовывались** перед тренировкой
- **Mapping**: `utils/pose_processor.py:convert_to_mini_rgbd()` (строки 96-180)
- **Процесс**:
  - MediaPipe 33 точки → MINI-RGBD 25 суставов
  - Используются основные точки MediaPipe + вычисляются промежуточные (spine, neck, head)
  - Интерполяция для отсутствующих суставов

### Статистики тренировочных данных
- **Train**: 737 последовательностей
- **Val**: 171 последовательность
- **Test**: 171 последовательность
- **Статистики ошибок реконструкции на validation**:
  - Min: 0.852866
  - Max: 2.419759
  - Mean: 1.716148
  - Std: 0.375900

### Сохранение предобработанных данных
- **НЕТ**, предобработанные данные не сохраняются
- Данные обрабатываются "на лету" при каждом запуске обучения
- Это может быть проблемой для консистентности!

---

## 3. ПРО ФОРМАТЫ

### MediaPipe vs Модель
- **MediaPipe выдает**: 33 точки (landmarks)
- **Модель ожидает**: 25 суставов × 3 координаты = 75 признаков
- **Преобразование**: MediaPipe 33 → MINI-RGBD 25 происходит в `PoseProcessor`

### Mapping MediaPipe → MINI-RGBD
- **Основные точки**:
  - MP_NOSE (0) → head, noseVertex
  - MP_LEFT_SHOULDER (11) → leftShoulder
  - MP_RIGHT_SHOULDER (12) → rightShoulder
  - MP_LEFT_HIP (23) → leftThigh
  - MP_RIGHT_HIP (24) → rightThigh
  - И т.д.
- **Вычисляемые точки**:
  - hip_center = (left_hip + right_hip) / 2 → global, spine
  - shoulder_center = (left_shoulder + right_shoulder) / 2 → spine1, spine2, neck
- **Код**: `utils/pose_processor.py:convert_to_mini_rgbd()` (строки 139-180)

### Система координат
- **MediaPipe**: Нормализованные координаты [0, 1] для x, y; относительная глубина z
- **После нормализации**: Относительно торса, масштабированные по расстоянию между бедрами
- **Финальный формат**: Нормализованные координаты относительно торса

### Разница в системе координат
- **Потенциальная проблема**: MediaPipe использует нормализованные координаты [0, 1], а после нормализации относительно торса координаты могут быть в другом диапазоне
- **Важно**: При тренировке и инференсе должна применяться **одинаковая** нормализация

---

## 4. ПРО ИНФЕРЕНС

### Данные на входе модели при инференсе
- **Формат**: Тензор формы (N, 30, 75)
- **Процесс**:
  1. Видео → MediaPipe (33 точки)
  2. MediaPipe → MINI-RGBD (25 суставов)
  3. Нормализация относительно торса
  4. Создание последовательностей (30 кадров)
  5. Flatten: (30, 25, 3) → (30, 75)

### Применяется ли та же нормализация?
- **ДА**, используется тот же `PoseProcessor` с теми же параметрами:
  - `normalize: true`
  - `normalize_relative_to: "torso"`
- **Код**: `inference_gpu.py:process_video()` использует `pose_processor.process_keypoints()`

### Как вычисляется reconstruction error?
- **Формула**: MSE (Mean Squared Error) по всей последовательности
- **Код**: `models/anomaly_detector.py:compute_reconstruction_errors()` (строки 52-83)
- **Процесс**:
  1. Forward pass: `reconstructed, _ = model(batch)`
  2. MSE: `mse = F.mse_loss(batch, reconstructed, reduction="none")` → (batch, seq_len, input_size)
  3. Усреднение: `mse_per_sequence = mse.mean(dim=(1, 2))` → (batch,)
- **Единицы**: Квадрат разницы в нормализованных координатах

### Где хранится порог 2.35 и как получен?
- **Хранится**: `checkpoints/anomaly_detector.pt`
- **Метод получения**: 95-й перцентиль ошибок реконструкции на **validation данных**
- **Код**: `models/anomaly_detector.py:fit_threshold()` (строки 85-115)
- **Процесс**:
  1. Вычисляются ошибки реконструкции для всех validation последовательностей
  2. Берется 95-й перцентиль: `threshold = np.percentile(val_errors, 95)`
  3. Результат: 2.349035
- **Статистика validation ошибок**:
  - Min: 0.852866
  - Max: 2.419759
  - Mean: 1.716148
  - 95-й перцентиль: 2.349035

---

## 5. ПРО КОНКРЕТНЫЕ ФАЙЛЫ

### Checkpoints
- **best_model.pt** (4.15 MB):
  - Epoch: 4
  - Train loss: 0.987262
  - Val loss: 1.690814
  - Best val loss: 1.690814
  - Model state dict
  - Optimizer state dict
  - Config

- **anomaly_detector.pt** (1.38 MB):
  - Model state dict
  - Threshold: 2.349035
  - Threshold percentile: 95
  - Val errors: массив из 171 ошибки

- **checkpoint_epoch_10.pt**: Финальный checkpoint

### Лог тренировки
- **Файл**: Не сохраняется автоматически (только в консоль)
- **Информация из training_summary.md**: Детальная статистика по эпохам

### Конфигурационные параметры
- **Файл**: `config.yaml`
- **Ключевые параметры**:
  - input_size: 75
  - sequence_length: 30
  - normalize: true
  - normalize_relative_to: "torso"
  - encoder_hidden_sizes: [128, 64, 32]
  - decoder_hidden_sizes: [64, 128, 75]
  - threshold_percentile: 95

### Пример нормализованных данных
- **НЕТ**, примеры не сохраняются
- **Проблема**: Невозможно сравнить нормализованные данные тренировки и инференса

---

## 6. ПРО СТАТИСТИКИ

### Статистики тренировочных ошибок реконструкции
- **НЕ сохраняются** отдельно
- **Доступны только**: Train loss и Val loss по эпохам

### Статистики валидационных ошибок
- **Min**: 0.852866
- **Max**: 2.419759
- **Mean**: 1.716148
- **Std**: 0.375900
- **95-й перцентиль**: 2.349035 (порог)
- **Количество**: 171 последовательность

### Распределение ошибок
- **Гистограмма**: НЕ создается автоматически
- **Можно создать**: Используя `val_errors` из `anomaly_detector.pt`

### Почему порог 2.35, если val_loss был ~1.69?
- **КРИТИЧЕСКОЕ РАЗЛИЧИЕ**:
  - **val_loss** (1.69) - это средняя ошибка реконструкции на validation данных
  - **threshold** (2.35) - это 95-й перцентиль ошибок реконструкции
- **Объяснение**:
  - Среднее значение (mean) = 1.716
  - 95-й перцентиль = 2.349 (значит 5% последовательностей имеют ошибку > 2.35)
  - Это нормально: перцентиль всегда выше среднего для распределения с хвостом
- **Проблема при инференсе**:
  - Ошибка реконструкции на реальном видео: **2.997** (mean)
  - Это выше порога 2.35, поэтому все последовательности помечаются как аномальные
  - **Возможные причины**:
    1. Разные условия съемки (освещение, угол, расстояние)
    2. Разные возрастные группы младенцев
    3. Разные паттерны движений
    4. Проблемы с нормализацией (разные масштабы)

---

## 🔍 ВЫЯВЛЕННЫЕ ПРОБЛЕМЫ

### 1. Высокая ошибка реконструкции на реальном видео
- **Проблема**: Mean error = 2.997, все последовательности аномальные (100%)
- **Возможные причины**:
  - Разные условия съемки
  - Разные паттерны движений
  - Проблемы с нормализацией

### 2. Нет сохранения предобработанных данных
- **Проблема**: Невозможно сравнить данные тренировки и инференса
- **Решение**: Сохранять примеры нормализованных данных

### 3. Разница между val_loss и threshold
- **Проблема**: val_loss = 1.69, но threshold = 2.35
- **Объяснение**: Это нормально (перцентиль vs среднее)
- **Но**: Ошибка на реальном видео (2.997) значительно выше

### 4. Нет статистик тренировочных ошибок
- **Проблема**: Невозможно сравнить распределение ошибок

---

## 📋 РЕКОМЕНДАЦИИ

1. **Сохранять предобработанные данные** для сравнения
2. **Логировать статистики** ошибок реконструкции при тренировке
3. **Создавать гистограммы** распределения ошибок
4. **Сравнивать нормализацию** между тренировкой и инференсом
5. **Анализировать разницу** между MINI-RGBD данными и реальными видео

