# Руководство по тестированию системы

## 📋 Обзор

Это руководство описывает процесс тестирования системы детекции аномалий движений младенцев.

## 🎯 Компоненты тестирования

### 1. Синтетические аномалии

Создание синтетических аномалий для тестирования системы:

```python
from synthetic_anomalies import SyntheticAnomalyGenerator
import numpy as np

# Загружаем нормальную последовательность
normal_seq = np.load("path/to/normal_sequence.npy")  # [T, 75]

# Создаем генератор
generator = SyntheticAnomalyGenerator(normal_seq)

# Генерируем различные типы аномалий
asymmetry = generator.create_asymmetry(strength=0.3, side="left")
tremor = generator.create_tremor(frequency=10.0, amplitude=0.1)
rigidity = generator.create_rigidity(stiffness=0.5)
hyperactivity = generator.create_hyperactivity(intensity=0.3)
```

### 2. Комплексное тестирование

Запуск комплексного тестирования на всех доступных видео:

```bash
python run_comprehensive_test.py
```

**Результаты:**
- `results/comprehensive_test_results.json` - сводный отчет
- `results/test_*/` - результаты для каждого теста

### 3. Бенчмарк производительности

Измерение скорости инференса:

```bash
python optimize_performance.py
```

**Результаты:**
- `results/performance_benchmark.json` - результаты бенчмарка

### 4. Медицинский интерфейс

Запуск веб-интерфейса для анализа видео:

```bash
python medical_interface.py
```

Интерфейс будет доступен по адресу: `http://localhost:7860`

## 📁 Структура тестирования

```
test_videos/
├── normal/          # Нормальные видео
├── anomalies/        # Синтетические аномалии (.npy)
└── real/             # Реальные записи

results/
├── test_*/           # Результаты каждого теста
├── comprehensive_test_results.json
└── performance_benchmark.json
```

## 🔍 Типы тестов

### Нормальные движения

Тестирование на видео с нормальными движениями из MINI-RGBD:
- Ожидаемый результат: низкий уровень риска
- Anomaly score должен быть ниже порога

### Синтетические аномалии

1. **Асимметрия движений**
   - Ослабление движений одной стороны тела
   - Ожидаемый результат: обнаружение аномалии

2. **Тремор конечностей**
   - Мелкая дрожь в руках и ногах
   - Ожидаемый результат: обнаружение аномалии

3. **Ригидность**
   - Скованность движений
   - Ожидаемый результат: обнаружение аномалии

4. **Гиперкинез**
   - Избыточная активность
   - Ожидаемый результат: обнаружение аномалии

## 📊 Метрики оценки

### Точность детекции

- **True Positive (TP)**: Аномалия обнаружена правильно
- **True Negative (TN)**: Нормальное движение определено правильно
- **False Positive (FP)**: Ложное обнаружение аномалии
- **False Negative (FN)**: Пропущенная аномалия

**Точность** = (TP + TN) / (TP + TN + FP + FN)

### Производительность

- **FPS**: Кадров в секунду при инференсе
- **Throughput**: Последовательностей в секунду
- **Latency**: Задержка на одну последовательность

## 🚀 Быстрый старт

1. **Подготовка данных:**
   ```bash
   # Создайте папки для тестовых видео
   mkdir -p test_videos/normal test_videos/anomalies test_videos/real
   ```

2. **Генерация синтетических аномалий:**
   ```python
   python -c "from synthetic_anomalies import generate_anomalies_from_sequences; ..."
   ```

3. **Запуск тестирования:**
   ```bash
   python run_comprehensive_test.py
   ```

4. **Проверка результатов:**
   ```bash
   cat results/comprehensive_test_results.json
   ```

## ⚠️ Важные замечания

1. **GPU обязателен** для всех тестов
2. **Проверьте наличие checkpoint** перед запуском тестов
3. **Время обработки** зависит от длины видео и размера батча
4. **Реальные видео** требуют ручной оценки результатов

## 📝 Интерпретация результатов

### Низкий риск (Low Risk)
- Anomaly score < threshold
- Anomaly rate < 10%
- Рекомендация: Нормальные движения

### Средний риск (Medium Risk)
- Anomaly score ≈ threshold
- Anomaly rate 10-30%
- Рекомендация: Наблюдение

### Высокий риск (High Risk)
- Anomaly score > threshold * 1.5
- Anomaly rate > 30%
- Рекомендация: Консультация специалиста

## 🔧 Устранение неполадок

### Ошибка: "Checkpoint не найден"
```bash
# Убедитесь, что модель обучена
ls checkpoints/anomaly_detector.pt
```

### Ошибка: "GPU недоступен"
```bash
# Проверьте доступность GPU
python -c "import torch; print(torch.cuda.is_available())"
```

### Медленная обработка
- Уменьшите batch_size в config.yaml
- Используйте меньше кадров для тестирования
- Проверьте использование GPU (nvidia-smi)

## 📚 Дополнительные ресурсы

- `TRAINING_README.md` - Руководство по обучению
- `training_summary.md` - Сводка результатов обучения
- `config.yaml` - Конфигурация системы

