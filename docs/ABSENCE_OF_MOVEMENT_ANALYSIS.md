# АНАЛИЗ: ОТСУТСТВИЕ ДВИЖЕНИЙ КАК АНОМАЛИЯ

## Ответ: ДА, отсутствие движений считается аномалией

### Как система определяет отсутствие движений:

#### 1. **Порог детекции:**
- **Снижение амплитуды > 2σ (стандартных отклонений)** от нормы
- **Код:** `utils/anomaly_analyzer.py:354-356`
  ```python
  amplitude_z_scores = (normal_amplitude - anomalous_amplitude) / (normal_amplitude_std + 1e-6)
  low_amplitude_joints = np.where(amplitude_z_scores > 2.0)[0]  # > 2 sigma снижение
  ```

#### 2. **Нормальные значения из тренировочных данных:**
- **Средняя амплитуда:** 0.072949 ± 0.040257
- **Средняя скорость:** 0.039273 ± 0.020059
- **Источник:** `checkpoints/normal_statistics.json` (737 последовательностей из MINI-RGBD)

#### 3. **Что считается "отсутствием движений":**

**Для отдельных суставов:**
- Если амплитуда сустава снижена более чем на **2σ** от нормы
- Пример: если нормальная амплитуда `leftHand` = 0.125, а в видео = 0.025
  - Снижение: (0.125 - 0.025) / 0.047 = 2.13σ → **АНОМАЛИЯ**
  - Процент снижения: (1 - 0.025/0.125) × 100% = **80%**

**Для общей амплитуды:**
- Если общая амплитуда снижена более чем на **2σ**
- Пример: норма = 0.0729, видео = 0.0200
  - Снижение: (0.0729 - 0.0200) / 0.0403 = 1.31σ → **НЕ аномалия** (нужно > 2σ)
  - Но если видео = 0.0100:
  - Снижение: (0.0729 - 0.0100) / 0.0403 = 1.56σ → **НЕ аномалия** (все еще < 2σ)
  - Если видео = 0.0000 (полное отсутствие):
  - Снижение: (0.0729 - 0.0000) / 0.0403 = 1.81σ → **НЕ аномалия** (нужно > 2σ)

**Важно:** Полное отсутствие движений (амплитуда = 0) даст z-score ≈ 1.81σ, что **меньше порога 2.0σ**!

### Проблема текущей реализации:

#### ❌ **КРИТИЧЕСКАЯ ПРОБЛЕМА:**
Система анализирует сниженную амплитуду **ТОЛЬКО** если:
1. Есть аномальные последовательности (reconstruction error > threshold)
2. `risk_level != "low"`

**Код:** `inference_advanced.py:212-230`
```python
if sequences_array is not None and len(sequences_array) > 0 and risk_level != "low":
    detailed_analysis = analyze_joint_errors(...)
```

**Это означает:**
- Если ребенок **не двигается вообще**, ошибка реконструкции может быть **низкой** (модель хорошо реконструирует статичную позу)
- В этом случае `risk_level = "low"` → детальный анализ **НЕ выполняется**
- **Отсутствие движений НЕ будет обнаружено!**

### Что показывают реальные данные:

#### Пример 1: `analysis_baby` (нормальные движения)
- **Ошибка реконструкции:** 0.006339 (очень низкая)
- **Аномалий:** 0%
- **Детальный анализ:** `{}` (пустой, т.к. risk_level = "low")
- **Вывод:** Ребенок двигается нормально, система работает корректно

#### Пример 2: `baby_test2.mp4` (старая модель?)
- **Ошибка реконструкции:** 10.128 (очень высокая)
- **Аномалий:** 100%
- **Проблема:** Используется старый порог (2.349) вместо нового (0.020204)
- **Вывод:** Возможно, это результат старой модели или другого порога

### Рекомендации:

#### 1. **Исправить логику детекции отсутствия движений:**
```python
# ДОЛЖНО быть:
if sequences_array is not None and len(sequences_array) > 0:
    # Анализировать ВСЕГДА, даже если risk_level = "low"
    detailed_analysis = analyze_joint_errors(...)
    
    # Проверять сниженную амплитуду отдельно
    if detailed_analysis.get("amplitude_analysis", {}).get("has_amplitude_anomalies"):
        # Даже при низкой ошибке реконструкции, сниженная амплитуда = аномалия
        risk_level = "medium" or "high"
```

#### 2. **Добавить специальную проверку на полное отсутствие движений:**
```python
# Проверка на полное отсутствие движений
total_amplitude = np.mean(anomalous_amplitude)
if total_amplitude < 0.01:  # Практически нет движений
    findings.append({
        "type": "absence_of_movement",
        "description": "Критически низкая амплитуда движений (возможно полное отсутствие)",
        "severity": "high",
        "data": {
            "amplitude": float(total_amplitude),
            "normal_amplitude": float(total_normal_amplitude),
        }
    })
```

#### 3. **Использовать абсолютные пороги для критических случаев:**
- Если амплитуда < 0.01 → **ВСЕГДА аномалия** (независимо от z-score)
- Если амплитуда < 0.005 → **КРИТИЧЕСКАЯ аномалия**

### Выводы:

✅ **Да, отсутствие движений считается аномалией** (при снижении > 2σ)

❌ **НО:** Текущая реализация **НЕ обнаруживает** отсутствие движений, если:
- Ошибка реконструкции низкая (модель хорошо реконструирует статичную позу)
- `risk_level = "low"` → детальный анализ не выполняется

⚠️ **Проблема:** Полное отсутствие движений может дать z-score < 2.0σ, если нормальная амплитуда имеет большой разброс

🔧 **Решение:** Добавить абсолютные пороги и анализировать амплитуду независимо от ошибки реконструкции

