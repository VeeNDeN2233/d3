# Система аутентификации и авторизации

## Обзор

Система аутентификации обеспечивает безопасный доступ к медицинскому интерфейсу для анализа видео младенцев. Все функции анализа защищены и требуют авторизации.

## Компоненты

### 1. База данных пользователей (`auth/database.py`)

- **SQLite база данных** для хранения пользователей и сессий
- **Хеширование паролей** с использованием SHA-256 и соли
- **Управление сессиями** с автоматическим истечением (24 часа)
- **Роли пользователей**: `user` (по умолчанию), `admin`

### 2. Менеджер аутентификации (`auth/auth_manager.py`)

- Регистрация новых пользователей
- Вход в систему
- Выход из системы
- Проверка авторизации
- Управление сессиями

### 3. Интеграция в Gradio (`medical_interface.py`)

- Вкладка "🔐 Вход/Регистрация" для управления аккаунтами
- Автоматическая проверка авторизации при анализе видео
- Отображение информации о текущем пользователе
- Защита всех функций анализа

## Использование

### Регистрация

1. Перейдите на вкладку "🔐 Вход/Регистрация"
2. Заполните форму регистрации:
   - **Имя пользователя**: минимум 3 символа
   - **Пароль**: минимум 6 символов
   - **Подтверждение пароля**: должно совпадать с паролем
   - **Email** (опционально)
   - **Полное имя** (опционально)
3. Нажмите "Зарегистрироваться"
4. После успешной регистрации вы автоматически войдете в систему

### Вход в систему

1. Перейдите на вкладку "🔐 Вход/Регистрация"
2. Введите имя пользователя и пароль
3. Нажмите "Войти"
4. После успешного входа вы сможете использовать функции анализа

### Выход из системы

1. На вкладке "🔐 Вход/Регистрация" нажмите кнопку "Выйти"
2. Сессия будет завершена, и вам потребуется снова войти для использования функций

### Анализ видео

- Все функции анализа видео требуют авторизации
- Если вы не авторизованы, при попытке анализа вы получите сообщение с просьбой войти в систему
- После входа вы сможете использовать все функции анализа

## Безопасность

### Хранение паролей

- Пароли никогда не хранятся в открытом виде
- Используется хеширование SHA-256 с уникальной солью для каждого пользователя
- Формат хранения: `salt:hash`

### Сессии

- Каждая сессия имеет уникальный токен (32 байта, URL-safe)
- Сессии автоматически истекают через 24 часа
- Токены хранятся в базе данных с привязкой к пользователю

### Защита функций

- Все функции анализа проверяют авторизацию перед выполнением
- Неавторизованные пользователи не могут использовать функции анализа
- Токен сессии передается через State компонент Gradio

## Структура базы данных

### Таблица `users`

- `id`: INTEGER PRIMARY KEY
- `username`: TEXT UNIQUE (имя пользователя)
- `email`: TEXT UNIQUE (опционально)
- `password_hash`: TEXT (хеш пароля с солью)
- `full_name`: TEXT (опционально)
- `role`: TEXT DEFAULT 'user' (роль: user, admin)
- `created_at`: TIMESTAMP
- `last_login`: TIMESTAMP
- `is_active`: INTEGER DEFAULT 1 (активен ли аккаунт)

### Таблица `sessions`

- `id`: INTEGER PRIMARY KEY
- `user_id`: INTEGER (FK к users.id)
- `session_token`: TEXT UNIQUE (токен сессии)
- `created_at`: TIMESTAMP
- `expires_at`: TIMESTAMP (время истечения)

## Технические детали

### Расположение файлов

- База данных: `auth/users.db` (создается автоматически)
- Модули: `auth/database.py`, `auth/auth_manager.py`
- Интеграция: `medical_interface.py`

### Зависимости

- `sqlite3` (встроенный в Python)
- `hashlib` (встроенный в Python)
- `secrets` (встроенный в Python)
- `gradio` (для интерфейса)

## Расширение функциональности

### Добавление ролей

Для добавления новых ролей:

1. Обновите валидацию в `auth_manager.py`
2. Добавьте проверки ролей в защищенные функции
3. Обновите интерфейс для отображения ролей

### Административные функции

Для добавления административных функций:

1. Используйте `require_role(session_token, "admin")` для проверки прав
2. Создайте отдельную вкладку для администраторов
3. Добавьте функции управления пользователями

### Дополнительная безопасность

Рекомендуемые улучшения:

- Двухфакторная аутентификация (2FA)
- Ограничение попыток входа
- Логирование действий пользователей
- Шифрование базы данных
- HTTPS для продакшн окружения

